<h1>Ajouter un commentaire pour <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @rating] do |f| %>
  <div class="form-group">
    <%= f.input :content, label: 'Commentaire', as: :text, input_html: { rows: 5 }, placeholder: 'Écrivez votre commentaire ici' %>
  </div>

  <div class="form-group">
    <%= f.input :rating, label: 'Note', collection: 0..5, prompt: "Sélectionnez une note" %>
  </div>

  <div class="form-group">
    <%= f.button :submit, 'Ajouter le commentaire', class: 'btn btn-primary' %>
  </div>
<% end %>
